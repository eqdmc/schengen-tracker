# Managed by eqdmc/.github — do not edit. Changes overwritten on next sync.
name: "Agent: Review PR"

on:
  pull_request:
    types: [opened, synchronize, reopened, labeled]

permissions:
  pull-requests: write
  contents: read

jobs:
  review:
    if: contains(github.event.pull_request.labels.*.name, 'agent-work')
    uses: eqdmc/.github/.github/workflows/_agent-review-pr.yml@dd72949cef701b798b19519cc69455e02a12f866 # v0.2.1
    with:
      pr-number: ${{ github.event.pull_request.number }}
    secrets: inherit