# Managed by eqdmc/.github — do not edit. Changes overwritten on next sync.
name: PR Merged

on:
  pull_request_target:
    types: [closed]

permissions:
  pull-requests: write
  contents: read

jobs:
  outcome:
    uses: eqdmc/.github/.github/workflows/outcome-comment.yml@dd72949cef701b798b19519cc69455e02a12f866 # v0.2.1
    secrets: inherit

  update-queued:
    uses: eqdmc/.github/.github/workflows/auto-update-branch.yml@dd72949cef701b798b19519cc69455e02a12f866 # v0.2.1
    secrets: inherit