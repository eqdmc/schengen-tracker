# Managed by eqdmc/.github — do not edit. Changes overwritten on next sync.
name: Issue validation

on:
  issues:
    types: [opened, edited, reopened]

permissions:
  issues: write
  contents: read

jobs:
  validate:
    uses: eqdmc/.github/.github/workflows/issue-validate.yml@0e2e6782bb211966f41570a030a88b64257d2ff7 # v0.2.0
    secrets: inherit