# Managed by eqdmc/.github — do not edit. Changes overwritten on next sync.
name: Issue validation

on:
  issues:
    types: [opened, edited, reopened]

permissions:
  issues: write
  contents: read

jobs:
  validate:
    uses: eqdmc/.github/.github/workflows/issue-validate.yml@dd72949cef701b798b19519cc69455e02a12f866 # v0.2.1
    secrets: inherit