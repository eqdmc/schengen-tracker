# Managed by eqdmc/.github — do not edit. Changes overwritten on next sync.
name: Issue validation

on:
  issues:
    types: [opened, edited, reopened]

permissions:
  issues: write
  contents: read

jobs:
  validate:
    uses: eqdmc/.github/.github/workflows/issue-validate.yml@b087f95132c12c9b5858e3ab8a1449e188a189c5 # v0.1.0
    secrets: inherit