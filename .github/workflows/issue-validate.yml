# Managed by eqdmc/.github — do not edit. Changes overwritten on next sync.
name: Issue validation

on:
  issues:
    types: [opened, edited, reopened]

permissions:
  issues: write
  contents: read

jobs:
  validate:
    uses: eqdmc/.github/.github/workflows/issue-validate.yml@22772786de998dd16ef78b8cc7f87eac5be46457 # v0.2.1
    secrets: inherit