# Managed by eqdmc/.github — do not edit. Changes overwritten on next sync.
name: PR Check

on:
  pull_request:
    branches: [main]
    types: [opened, synchronize, reopened, labeled, unlabeled]

permissions:
  contents: write
  pull-requests: write
  issues: read

jobs:
  pr-metadata:
    uses: eqdmc/.github/.github/workflows/pr-metadata-check.yml@dd72949cef701b798b19519cc69455e02a12f866 # v0.2.1
    secrets: inherit

  auto-label:
    uses: eqdmc/.github/.github/workflows/auto-label.yml@dd72949cef701b798b19519cc69455e02a12f866 # v0.2.1
    secrets: inherit

  deps-vetting:
    uses: eqdmc/.github/.github/workflows/deps-vetting.yml@dd72949cef701b798b19519cc69455e02a12f866 # v0.2.1
    secrets: inherit
