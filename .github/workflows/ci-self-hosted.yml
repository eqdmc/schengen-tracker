# Deployed from eqdmc/.github — calls reusable self-hosted CI.
# Uses eqdmc-build runner group for faster CI.
name: ci

on:
  pull_request:
    branches: [main]
  push:
    branches: [main]
  merge_group:

permissions:
  contents: read

jobs:
  ci:
    uses: eqdmc/.github/.github/workflows/ci-self-hosted.yml@dd72949cef701b798b19519cc69455e02a12f866
    secrets: inherit