name: ci
on:
  pull_request:
    branches: [main]
  push:
    branches: [main]
  merge_group:
permissions:
  contents: read
jobs:
  ci:
    uses: eqdmc/.github/.github/workflows/ci.yml@main
    secrets: inherit