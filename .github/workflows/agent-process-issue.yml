# Managed by eqdmc/.github — do not edit. Changes overwritten on next sync.
name: "Agent: Process Issue"

on:
  issues:
    types: [labeled]

permissions:
  issues: write
  contents: read

jobs:
  process:
    if: github.event.label.name == 'agent-work'
    uses: eqdmc/.github/.github/workflows/_agent-process-issue.yml@dd72949cef701b798b19519cc69455e02a12f866 # v0.2.1
    with:
      issue-number: ${{ github.event.issue.number }}
    secrets: inherit